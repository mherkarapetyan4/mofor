import React, { PureComponent } from "react";
import styled from "styled-components";

class UserInfo extends PureComponent {
    render() {
        return <Wrapper>user info</Wrapper>;
    }
}

const Wrapper = styled.div``;

export default UserInfo;
